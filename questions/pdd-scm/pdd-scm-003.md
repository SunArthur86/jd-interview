---
id: pdd-scm-003
difficulty: L4
category: pdd-scm
subcategory: 商品
tags:
- 拼多多
- 供应链
- DDD
- 领域驱动
- 商品
feynman:
  essence: DDD 把复杂业务拆成"限界上下文"，每个上下文有统一的领域语言和模型，通过聚合根保证业务不变性，用领域事件解耦上下文——让供应链这种"商品/库存/订单/结算"高度耦合的系统有清晰的边界。
  analogy: DDD 像把一家大公司按业务拆成事业部（限界上下文）——每个事业部有自己的术语（统一语言）和核心业务（聚合根），部门间用正式文件（领域事件）沟通而非私下串门。
  first_principle: 复杂业务的根本问题是"概念混乱"（商品在采购、销售、仓储视角含义不同），DDD 用限界上下文划定概念边界，用聚合根保证业务规则原子性。
  key_points:
  - 战略设计：限界上下文、上下文映射、领域划分
  - 战术设计：实体、值对象、聚合根、领域服务、领域事件
  - 聚合根：一致性边界，外部只能持有引用不能直接改
  - 领域事件：解耦上下文，异步通知
first_principle:
  problem: 供应链涉及商品/供应商/采购/仓储/结算多业务，概念交叉耦合，如何划清边界让系统可演进？
  axioms:
  - 同一术语在不同业务语境含义不同（"商品"在采购=待采品，在仓储=SKU）
  - 业务规则需要原子性保证（下单必扣库存）
  - 上下文间应松耦合
  rebuild: 限界上下文划边界（商品/库存/订单独立）+ 聚合根管一致性（订单聚合根管订单+订单项）+ 领域事件解耦（订单创建事件触发库存扣减）。
follow_up:
- 聚合根怎么设计？——按一致性边界，一个聚合一次事务；订单+订单项是一个聚合，商品是另一个
- 领域事件怎么落地？——本地事件表 + MQ 保证最终一致
- DDD 和三层架构区别？——DDD 把"领域逻辑"从 Service 抽到 Domain 层，Service 变薄
memory_points:
- DDD 战略：限界上下文（边界）+ 上下文映射（关系）
- DDD 战术：实体/值对象/聚合根/领域服务/领域事件
- 聚合根 = 一致性边界，外部只引用不直改
- 领域事件 + 本地消息表 = 最终一致
---

# 【拼多多供应链】DDD 怎么落地？供应链的领域模型怎么设计？

> JD 依据："应用软件设计原则、设计模式、领域驱动等设计和研发高质量的业务系统"。

## 一、为什么供应链需要 DDD

供应链的复杂性：
- **概念交叉**："商品"在采购=待采品，在仓储=SKU，在销售=SPU，在结算=计价品
- **业务规则多**：下单扣库存、退款回库存、预售锁库存
- **跨团队协作**：商品团队、库存团队、订单团队各自维护系统

不用 DDD 的问题：所有逻辑堆在 Service 层，概念混乱，改一处影响全局。

## 二、战略设计：限界上下文

```
供应链域（子域划分）
├─ 商品上下文（核心）：SPU/SKU/类目/属性
├─ 库存上下文（核心）：现货/预售/在途/冻结
├─ 采购上下文（支撑）：采购单/供应商/到货
├─ 仓储上下文（支撑）：入库/出库/调拨
├─ 订单上下文（核心）：订单/订单项/状态机
├─ 结算上下文（支撑）：对账/发票/付款
└─ 物流上下文（支撑）：运单/轨迹/签收
```

**上下文映射**（关系）：
- 订单 → 库存：订单创建触发库存扣减（领域事件）
- 采购 → 仓储：采购到货触发出库（领域事件）
- 商品 ← 订单：订单引用商品 SKU（防腐层 ACL 转换）

## 三、战术设计：聚合根

**聚合根 = 一致性边界**。一个聚合内的修改必须原子，跨聚合用最终一致。

```
订单聚合根
├─ Order（聚合根，实体）
│   ├─ orderId（值对象）
│   ├─ status（枚举：待支付/已支付/已发货/已完成）
│   └─ 地址（值对象）
├─ OrderItem（实体，属于订单聚合）
│   ├─ skuId
│   ├─ quantity
│   └─ price
└─ 业务规则：订单总金额 = Σ 订单项金额；状态变更必须按状态机

商品聚合根（独立聚合）
├─ Product（聚合根）
│   ├─ spuId
│   ├─ skus[]  （实体）
│   └─ 属性（值对象）
```

**关键原则**：
- 外部只能持有聚合根引用（`orderId`），不能直接持有 `OrderItem`
- 修改聚合内的操作必须通过聚合根方法（`order.addItem()` 而非 `orderItem.setQty()`）

## 四、领域事件解耦

```java
// 订单聚合根发布事件
public class Order extends AggregateRoot {
    public void pay() {
        this.status = PAID;
        // 发布领域事件（同事务写入本地事件表）
        DomainEvents.publish(new OrderPaidEvent(this.orderId, this.items));
    }
}

// 库存上下文订阅（异步，最终一致）
@EventHandler
public class StockEventHandler {
    public void on(OrderPaidEvent event) {
        for (Item item : event.getItems()) {
            stockService.deduct(item.getSkuId(), item.getQuantity());
        }
    }
}
```

**落地**：本地消息表 + MQ（保证事件不丢）：
```java
@Transactional
public void pay(Long orderId) {
    orderDao.updateStatus(orderId, PAID);
    outboxDao.insert(new Outbox("OrderPaidEvent", payload));  // 同事务写事件表
}
// 定时扫 outbox 投递 MQ，成功删除
```

## 五、供应链 DDD 落地的难点

1. **聚合粒度难定**：订单+订单项是一个聚合，但订单+物流是一个吗？（通常拆开，物流独立聚合）
2. **跨聚合事务**：下单扣库存不能强一致（跨服务），用Saga/TCC补偿
3. **历史系统改造**：从贫血模型到充血模型要重构，建议新业务先试点
4. **团队认知**：DDD 学习曲线陡，需要培训

## 六、和传统三层的对比

| 维度 | 三层架构 | DDD |
|------|---------|-----|
| 领域逻辑 | 堆在 Service | 抽到 Domain 层（充血模型）|
| Service | 胖（业务+技术） | 瘦（只编排，无业务）|
| 一致性 | 事务包一大堆 | 聚合内强一致，跨聚合最终一致 |
| 演进 | 改一处影响全局 | 上下文独立演进 |

## 七、底层本质：复杂度的"分而治之"

DDD 本质是**"用业务语言驱动技术建模"**：
- 业务方说"下单扣库存" → 技术方建模为"订单聚合发布 OrderPaidEvent，库存聚合订阅扣减"
- 概念有边界（限界上下文）
- 一致性有边界（聚合根）
- 协作有契约（领域事件）

**这是软件工程"关注点分离"在业务建模的体现**——让复杂的供应链系统像乐高积木一样，每块（上下文）职责清晰、接口明确。

## 常见考点
1. **聚合和聚合根区别**？——聚合是一组相关对象（一致性边界），聚合根是聚合的入口实体。
2. **DDD 一定比三层架构好吗**？——不是。简单 CRUD 用 DDD 过度设计；复杂业务领域才值得。
3. **领域事件怎么保证不丢**？——本地消息表（业务+事件同事务）+ MQ + 消费幂等。

## 苏格拉底式面试追问

> 这组追问不是背答案，而是模拟面试官层层逼近本质。每一问先回答"为什么"，再回答"怎么做"，最后回答"如何证明"。

### 第一层：目标与动机

**Q：拼多多供应链已经有商品/库存/订单三套独立的微服务，为什么还要用 DDD 重新建模？现有的三层架构 + RPC 调用哪里出了问题？**

出在"概念穿透"和"逻辑散落"。现有系统的问题：订单服务里 `OrderService.create()` 直接写了库存校验逻辑（`if (stock < qty) throw`），因为业务方图省事把库存判断 copy 进来。结果是库存规则改了（比如加上"预售允许超卖"），订单服务也得改，耦合扩散。DDD 的动机是用限界上下文划死边界——库存规则只能在库存上下文里，订单只能通过 `StockReservedEvent` 感知结果，物理隔离业务逻辑的蔓延。

### 第二层：证据与定位

**Q：你怎么判断"订单服务里偷偷写了库存逻辑"这种越界？代码 review 能发现吗？**

Code Review 不够，要靠架构守护工具：
1. **ArchUnit** 写测试规则——`classes().that().resideInAPackage("..order..").should().onlyDependOnClassesThat().resideInAnyPackage("..order..", "..event..")`，订单包不能直接 import 库存包的类，跑单测就 fail。
2. **依赖矩阵扫描**——用 `jqassistant` 或 Structure101 扫描，看 `com.pdd.order` 是否有对 `com.pdd.stock.service.StockService` 的直接依赖，画出依赖图，发现违规连线。
3. **领域语言对齐检查**——在订单服务的代码里 grep `stock` 关键字，如果出现 `stockDao`/`stockService` 直接调用，就是越界证据。

### 第三层：根因深挖

**Q：你用聚合根把订单的一致性边界划好了，但下单扣库存这个跨聚合操作，事务怎么保证？订单创建成功但库存扣减失败怎么办？**

不能用本地事务（跨服务跨库）。用 Saga 模式 + 本地消息表。具体流程：
1. 订单聚合 `create()` 时，同事务写 `order` 表 + `outbox` 表（`OrderCreatedEvent`），本地事务保证订单和事件原子。
2. 定时任务扫 outbox 投递 Kafka，库存服务消费 `OrderCreatedEvent` 调 `stock.deduct()`。
3. 如果库存不足扣减失败，库存服务发 `StockDeductFailedEvent`，订单服务消费后做补偿——`order.compensate()` 把订单状态回滚到 `CANCELLED`，并发 `OrderCancelledEvent`。根因解决靠的是"事件驱动 + 补偿事务"，不是分布式锁或 XA。

**Q：那为什么不直接用 TCC？Try-Confirm-Cancel 不是更严格吗？**

TCC 更严格（每个服务实现 Try/Confirm/Cancel 三个接口，Try 阶段就预扣资源），但有代价：
1. **业务侵入重**——库存服务要拆成 `tryDeduct`（冻结）、`confirmDeduct`（确认）、`cancelDeduct`（解冻），每个业务动作都要写三遍。
2. **不适合长流程**——拼多多下单到支付可能间隔几十分钟（用户犹豫），TCC 的 Try 资源冻结这么久会严重影响库存可用性。
3. **Saga 更适合**——订单先创建（不冻结库存），支付时才扣减，失败就补偿取消订单。只有"强资源锁定"场景（如机票座位）才用 TCC。供应链下单用 Saga 是因为库存可以短暂超卖（用户支付前），补偿成本低。

### 第四层：方案权衡

**Q：你用本地消息表 + Kafka 保证事件不丢，但如果 Kafka 消费者处理 `OrderCreatedEvent` 时宕机，重启后怎么保证幂等不重复扣库存？**

靠幂等键 + 状态机。库存扣减的幂等设计：
1. **幂等键**：`deduct_id = orderId + skuId`（事件携带），库存表有唯一索引 `(orderId, skuId)`，重复消费时 INSERT 冲突直接忽略。
2. **状态机校验**：库存扣减记录有状态 `INIT → DEDUCTED → RELEASED`，消费时先 `SELECT ... WHERE order_id=? AND status='INIT'`，只有 INIT 状态才扣，重复消费发现已是 `DEDUCTED` 直接 return。
3. **Redis 去重前置**：消费前 `SETNX order:{orderId}:deducted 1 EX 86400`，已存在就跳过，挡住 Kafka 至少一次的重复投递。

**Q：为什么不直接用 Kafka 的 exactly-once 语义？**

Kafka 的 EOS（`exactly.once.v2=true`）只保证"消费-处理-生产"链路的精确一次（用事务把 offset 提交和下游生产绑定）。但库存扣减是"消费 Kafka + 写 MySQL"，跨 Kafka 和 MySQL 两个系统，Kafka 事务管不到 MySQL。所以业务层必须自己做幂等（唯一键 + 状态机），EOS 只是减少重复投递，不能替代幂等设计。

### 第五层：验证与沉淀

**Q：你重构后，怎么证明限界上下文的边界真的守住了、没有新的越界代码混进来？**

靠 ArchUnit 持续守护：
1. **CI 集成**——ArchUnit 测试纳入 CI 流水线，任何 PR 引入越界依赖（订单 import 库存）直接 fail build。
2. **依赖度量**——每周跑一次依赖扫描，看"跨上下文调用次数"指标，应该是 0 或仅通过 event 包。如果出现非 0，告警 review。
3. **事件契约测试**——用 Pact 或 Spring Cloud Contract 做消费者驱动契约测试，订单发布的 `OrderCreatedEvent` schema 变更必须通过库存服务的契约测试，防止接口悄悄改动。

**Q：DDD 重构后怎么让团队维护这套模型，而不是半年后又退化成大泥球？**

三件事：
1. **统一语言词典**——维护一份领域术语表（"商品=SPU，库存=可售现货+预售"，每个术语绑定上下文），代码命名必须用词典术语，PR review 对照。
2. **聚合根守护规则**——Code Review checklist：聚合内的修改必须通过聚合根方法（`order.addItem()`），禁止直接 `orderItem.setQty()`；外部只持有 `orderId` 不持有 `Order` 引用。
3. **建模工作坊**——每个迭代做一次事件风暴（Event Storming），业务方+研发一起贴便利贴梳理领域事件，让模型持续对齐业务变化，而不是一次性设计完就冻结。

## 结构化回答

**30 秒电梯演讲：** 供应链涉及商品/供应商/采购/仓储/结算多业务，概念交叉耦合，如何划清边界让系统可演进？简单说就是——DDD 把复杂业务拆成"限界上下文"，每个上下文有统一的领域语言和模型，通过聚合根保证业务不变性，用领域事件解耦上下文。

**展开框架：**
1. **战略设计** — 战略设计：限界上下文、上下文映射、领域划分
2. **战术设计** — 战术设计：实体、值对象、聚合根、领域服务、领域事件
3. **聚合根** — 聚合根：一致性边界，外部只能持有引用不能直接改

**收尾：** 您看这块要不要再展开聊聊？

## 视频脚本

> 预计时长：3 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：DDD 怎么落地？供应链的领域模型怎么设计？ | 今天聊「DDD 怎么落地？供应链的领域模型怎么设计？」。一句话：DDD 把复杂业务拆成"限界上下文"，每个上下文有统一的领域语言和模型，通过聚合根保证业务不变性，用领域事件解耦上下文 | 开场钩子 |
| 0:12 | 核心概念图 + 关键词浮现 | 要点是：战略设计：限界上下文、上下文映射、领域划分 | 核心概念 |
| 0:51 | 能力/参数拆解表 | 要点是：战术设计：实体、值对象、聚合根、领域服务、领域事件 | 能力拆解 |
| 1:30 | 流程图：输入→处理→输出 | 要点是：聚合根：一致性边界，外部只能持有引用不能直接改 | 关键机制 |
| 2:09 | 代码片段 + 注释高亮 | 要点是：领域事件：解耦上下文，异步通知 | 实战要点 |
| 3:00 | 总结卡 + 下期预告 | 记住核心要点就够了。下期见。 | 收尾 |
