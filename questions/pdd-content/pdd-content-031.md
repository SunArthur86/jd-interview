---
id: pdd-content-031
difficulty: L4
category: pdd-content
subcategory: Agent 改造
tags:
- 拼多多
- 内容
- AI Agent
- 内容审核
- LLM
- 多模态
feynman:
  essence: AI Agent 改造内容审核是"用 LLM+工具调用替代部分规则/模型审核"——LLM 理解语义+多模态+推理，机审覆盖率从 80% 提到 95%+，人审大幅减少。
  analogy: 传统审核像查字典（规则），AI Agent 像读全文理解意思（LLM）+必要时翻资料（工具调用）。
  first_principle: 规则审核覆盖窄、模型审核贵且不灵活，LLM 理解+推理+工具可大幅提升审核效果。
  key_points:
  - LLM 替代规则+部分模型（语义理解）
  - 多模态（图/视频/音频）
  - 工具调用（敏感词库/黑名单查询）
  - Agent 流程：感知→决策→执行
  - 人审回流持续优化
first_principle:
  problem: 传统审核覆盖窄+成本高，如何用 LLM Agent 提效果降成本？
  axioms:
  - LLM 理解语义强
  - 多模态融合好
  - 工具调用扩展能力
  rebuild: Agent（LLM+工具+流程）替代部分机审+人审。
follow_up:
  - LLM 审核延迟怎么降？——小模型蒸馏+批量推理+缓存
  - 怎么防 LLM 误判？——置信度阈值+人审兜底+对抗样本
  - 成本怎么控？——分级调用（简单规则，复杂 LLM）
memory_points:
  - Agent：LLM+工具+流程
  - 多模态：图/视频/音频
  - 工具：敏感词/黑名单/知识库
  - 落地：分级+人审兜底
---

# 【拼多多内容】AI Agent 怎么改造内容审核？

> JD 依据："和算法同学挖掘业务问题"、"评价和行家社区"、"系统架构优化"。

## 一、传统审核的局限

```
规则审核：
  - 只能匹配关键词/正则
  - 变体（谐音/拆字/暗语）漏判
  
模型审核：
  - 单任务（涉黄/广告分别训）
  - 改成本高（新违规要标数据+训练）
  - 多模态融合弱

人审：
  - 成本高
  - 慢
```

## 二、AI Agent 改造思路

```
传统：规则 → 模型 → 人工（漏斗）
Agent：规则 → Agent（LLM+工具+多模态）→ 人工（少量疑难）

Agent 能力：
  - LLM 理解语义（暗语/变体/上下文）
  - 多模态（图+文+视频一起判）
  - 工具调用（查敏感词库/黑名单/历史）
  - 推理（结合上下文判断意图）
```

## 三、Agent 架构

```
UGC 内容
   ↓
感知：提取文本/图片/视频/音频
   ↓
决策（LLM）：
  - 上下文理解（商品/用户/历史）
  - 多模态融合（图+文联合判）
  - 工具调用：
    * 查敏感词库（API）
    * 查用户黑名单（API）
    * 查相似历史违规（向量库）
    * 调小模型（涉黄/暴恐分类）
  - 输出：违规类型+置信度+理由
   ↓
执行：
  - 高置信度 → 直接决策（通过/拒绝）
  - 中置信度 → 人审
  - 低置信度 → 通过
```

## 四、Prompt 设计

```
你是拼多多内容审核 Agent。

任务：判断 UGC 内容是否违规。

违规类型：
  - 涉政/涉黄/暴恐/广告/辱骂/诱导/虚假

输入：
  - 文本：{content}
  - 图片描述：{image_caption}（视觉模型生成）
  - 上下文：商品={product}, 用户历史={history}

可用工具：
  - querySensitiveWords(text)：查敏感词库
  - queryBlacklist(uid)：查黑名单
  - searchSimilarCases(content)：查相似违规

输出（JSON）：
{
  "violated": true/false,
  "type": "广告/辱骂/...",
  "confidence": 0.0-1.0,
  "reason": "判断理由"
}

注意：
  - 暗语/谐音/拆字都算违规
  - 结合上下文判断（如商品是母婴，评价涉性是严重违规）
```

## 五、工具调用（Function Calling）

```python
tools = [
    {
        "name": "query_sensitive_words",
        "description": "查文本是否含敏感词",
        "parameters": {"text": "string"}
    },
    {
        "name": "search_similar_violations",
        "description": "向量检索相似违规案例",
        "parameters": {"content": "string", "top_k": "int"}
    }
]

# LLM 决定调哪个工具
response = llm.chat(messages, tools=tools)
# Agent 框架执行工具调用，结果返回 LLM 再决策
```

## 六、多模态审核

```
图片：
  - 视觉模型（CLIP/BLIP）生成描述
  - LLM 结合图文判断
  
视频：
  - 抽帧 → 每帧视觉模型
  - 关键帧 + 音频 ASR → LLM 联合判

音频：
  - ASR 转文字
  - 声纹识别（敏感人物）
  - LLM 判断文字
```

## 七、延迟与成本控制

**痛点**：LLM 推理慢（秒级）+贵，全量调不可承受。

**分级策略**：
```
1. 规则层（毫秒，便宜）：明显违规/正常直接判（80%）
2. 小模型层（百毫秒）：常见违规分类（15%）
3. LLM Agent（秒，贵）：仅疑难/高价值/新型（5%）
```

**优化**：
- 小模型蒸馏（学生模型）
- 批量推理（多条一起）
- 缓存（相似内容命中）
- 异步（不阻塞发布，先 status=0 后审）

## 八、防误判

```
1. 置信度阈值（>0.95 直接判，0.5-0.95 人审，<0.5 通过）
2. 对抗样本测试（红队定期攻击）
3. 人审兜底（关键场景必人审）
4. 用户申诉通道（误判可申诉）
5. 监控指标（误判率/漏判率）
```

## 九、反馈闭环

```
人审结果 → 标注库 → 
  - 加入 RAG 知识库（Agent 检索参考）
  - 微调 LLM（领域适配）
  - 规则补丁（新模式入库）
```

## 十、落地路径

```
阶段 1：LLM 辅助人审（生成审核建议，人确认）
阶段 2：LLM 处理疑难（规则+模型搞不定的）
阶段 3：LLM 替代部分模型（多模态融合）
阶段 4：全 Agent（LLM+工具+流程自动化）
```

## 十一、底层本质

AI Agent 改造审核本质是**"用 LLM 理解+推理+工具调用突破规则/单模型的局限"**——多模态融合+语义理解+工具扩展+人审兜底+反馈闭环；落地关键是分级（成本可控）+防误判+持续优化。

## 常见考点
1. **LLM 审核延迟怎么降**？——小模型蒸馏+分级调用+缓存+异步。
2. **怎么防 LLM 幻觉误判**？——置信度阈值+人审兜底+对抗样本+可解释（输出理由）。
3. **Agent 和传统模型审核怎么协同**？——规则→小模型→Agent→人审分级漏斗。
