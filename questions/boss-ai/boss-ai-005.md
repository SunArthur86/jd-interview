---
id: boss-ai-005
difficulty: L4
category: boss-ai
subcategory: 音视频任务
tags:
- 巨剧核
- AI 陪伴
- 音视频任务
- TTS
- ASR
- 数字人
- 异步任务编排
feynman:
  essence: "音视频 AI 生成是'用户请求 → 异步任务排队 → 多阶段算子编排（ASR/LLM/TTS/数字人渲染/视频合成）→ 结果回传'的长链路任务系统，核心是状态机和资源调度。"
  analogy: "像一条影视后期流水线——剧本（用户请求）进剪辑室（任务队列），分别过配音（TTS）、字幕（ASR）、特效（数字人）、合成（视频），每一步都有专门的工位（GPU 资源），最后成品通知客户取件（回调）。"
  first_principle: "音视频生成是耗时（秒到分钟级）、耗资源（GPU）、多阶段、易失败的异步过程，与用户的实时请求-响应模型完全不同。必须用'任务 + 状态机 + 队列 + 回调'重构交互模式。"
  key_points:
  - "任务模型：task_id + 类型（TTS/ASR/数字人/视频）+ 输入 + 状态 + 结果"
  - "状态机：创建→排队→运行→成功/失败/超时，可重试可取消"
  - "算子编排：DAG（如 ASR→LLM→TTS→渲染）有依赖有并行"
  - "资源调度：GPU 池化、优先级队列、抢占/预留"
  - "结果回传：回调 webhook + WebSocket 进度推送 + 轮询兜底"
  socratic:
  - "用户点'生成数字人视频'，几秒能出结果吗？为什么不能像聊天一样秒回？"
  - "既然要等几十秒，前端怎么知道进度？是轮询还是有更优雅的方式？"
  - "TTS 完成了但数字人渲染挂了，整个任务算成功还是失败？要不要重试？"
  - "GPU 资源有限，VIP 用户和免费用户排队，怎么调度才公平又赚钱？"
  - "任务跑了一半服务挂了，重启后怎么知道跑到哪一步、能不能续跑？"
first_principle:
  problem: "如何把耗时、耗资源、多阶段、易失败的音视频 AI 生成，变成可观测、可恢复、可调度、可计费的产品服务？"
  axioms:
  - "音视频生成耗时远超 HTTP 请求超时（秒-分钟级）"
  - "GPU 资源稀缺昂贵，必须池化调度"
  - "多阶段算子存在依赖关系和失败可能"
  rebuild: "把'请求-响应'重构为'任务-状态机'：用户提交任务拿 task_id，任务进队列由 worker 按算子 DAG 执行，状态全程持久化（断点可恢复），通过回调/推送通知结果，按资源优先级调度。"
follow_up:
- "任务超时怎么判定？——每类任务设 P99 阈值（如 TTS 30s，视频 5min），超时标记失败可重试；区分算子级和任务级超时。"
- "怎么防止用户重复提交？——幂等 key（同一 input+params 短期内复用结果）+ 结果缓存。"
- "GPU 怎么调度？——Kubernetes + GPU 设备插件，按任务类型分队列，VIP 高优先级，支持抢占。"
memory_points:
- "异步任务三件套：task_id + 状态机 + 队列"
- "算子编排用 DAG，有依赖有并行"
- "状态持久化才能断点恢复"
- "结果回传：回调 + 推送 + 轮询三选一或组合"
---

# 【巨剧核 AI 陪伴】音视频异步任务系统怎么设计？

> JD 依据："负责短剧视频服务端，AI 生成任务；熟悉音视频或 AI 生成异步任务。"

## 一、为什么不能同步等

```
同步请求：用户 → 服务端 → TTS+渲染+合成（30s-5min）→ 返回
后果：HTTP 超时 / 连接占用 / 无法扩容 / 体验崩
```

音视频生成的本质是**长耗时、重资源、多阶段**——与同步 HTTP 模型天然冲突，必须重构为异步任务。

## 二、任务模型

```
Task {
  task_id: 全局唯一
  type: TTS | ASR | DIGITAL_HUMAN | VIDEO_GEN | COMPOSITE
  input: { text / audio / image / params }
  status: CREATED | QUEUED | RUNNING | SUCCESS | FAILED | TIMEOUT | CANCELED
  progress: 0-100
  result_url: ...
  error: ...
  user_id, role_id, priority, idempotency_key
  created_at, updated_at, timeout_at
}
```

## 三、状态机

```
        ┌─────────┐
        │ CREATED │─── 创建并持久化
        └────┬────┘
             ↓
        ┌─────────┐
        │ QUEUED  │─── 入队等资源
        └────┬────┘
             ↓
        ┌─────────┐  ┌──────┐
        │ RUNNING │─→│ 失败 │──可重试──→ QUEUED
        └────┬────┘  └──────┘
             ↓
        ┌─────────┐
        │ SUCCESS │ / FAILED / TIMEOUT / CANCELED
        └─────────┘
```

**关键**：每次状态变更都持久化（DB + 日志），服务重启可从最后状态恢复。

## 四、算子编排（DAG）

复杂任务是多个算子的组合：
```
"用户发语音 → 数字人回复视频"
   ASR（语音转文字）
        ↓
   LLM（生成回复文本）
        ↓
   TTS（文本转语音）─────┐
        ↓                ↓
   渲染（数字人口型）   合成（视频+音频）
        ↓                ↓
        └──────→ 视频产出
```

用 DAG 描述：
- 节点 = 算子（带类型、参数、超时、重试策略）
- 边 = 依赖（前序输出 = 后序输入）
- 可并行的并行（如多语言 TTS 同时跑）

**框架**：Airflow / Temporal / 自研；AI 场景 Temporal（持久化执行）很合适。

## 五、队列与资源调度

```
多级队列：
  - VIP 队列（高优先级，预留 GPU）
  - 普通队列（按提交顺序）
  - 批量队列（低优先级，填空闲资源）

调度策略：
  - 优先级 + 公平调度（防止单用户占满）
  - 按算子类型分队列（TTS 池、渲染池分别调度）
  - 抢占：VIP 可抢占普通任务的 GPU
  - 弹性扩缩容：队列堆积触发扩容 GPU 节点
```

GPU 池化：K8s + device plugin，任务带 GPU 请求量，调度器匹配节点。

## 六、结果回传

```
[1] 回调 webhook：任务完成调用业务方注册的 callback URL
[2] WebSocket 推送：实时进度（10% → 30% → 完成）到前端
[3] 轮询兜底：前端按 task_id 查询（防推送丢失）
[4] 结果存储：对象存储（OSS/S3）+ CDN 加速下载
```

## 七、幂等与去重

```
idempotency_key = hash(input + params + user_id)
提交时检查：
  - 已有相同 key 的 SUCCESS → 直接返回缓存结果
  - 已有 RUNNING → 返回现有 task_id（不重复创建）
  - 无 → 创建新任务
```

防止用户连点、网络重传导致重复扣费、重复生成。

## 八、底层本质

音视频任务系统的本质是**"把同步的请求-响应，重构为异步的任务-状态机-回调"**：

- **解耦**：提交和执行分离（用户不等，服务慢慢算）
- **编排**：复杂流程用 DAG 描述（依赖、并行、失败重试）
- **调度**：稀缺 GPU 资源池化 + 优先级
- **可靠**：状态持久化 + 幂等 + 断点恢复

这是把'一次性函数调用'升级为'可观测、可恢复、可扩展的工业流水线'，是所有 AI 生成产品（Sora/Midjourney/数字人）共同的工程范式。

## 常见考点

1. **任务队列选 Kafka 还是 Redis 还是专门 MQ？**——Kafka 适合高吞吐流式，Redis/Redis Stream 简单灵活，RMQ/RocketMQ 强 ACK；AI 任务推荐专门的任务系统（Temporal）+ DB 状态持久化，纯 MQ 不够。
2. **进度怎么精确算？**——按 DAG 节点完成数加权（如 4 节点，每完成一个 +25%）；或在算子内部上报细粒度进度。
3. **GPU 任务怎么计费？**——按算子类型 + 执行时长 + 资源规格计费；预扣费（创建任务时锁额）+ 实际结算（完成时）+ 失败退还。


## 结构化回答

**30 秒电梯演讲：** 聊到音视频异步任务系统怎么设计，我的理解是——音视频 AI 生成是'用户请求 → 异步任务排队 → 多阶段算子编排（ASR/LLM/TTS/数字人渲染/视频合成）→ 结果回传'的长链路任务系统，核心是状态机和资源调度。打个比方，像一条影视后期流水线——剧本（用户请求）进剪辑室（任务队列），分别过配音（TTS）、字幕（ASR）、特效（数字人）、合成（视频），每一步都有专门的工位（GPU 资源），最后成品通知客户取件（回调）。

**展开框架：**
1. **任务模型** — task_id + 类型（TTS/ASR/数字人/视频）+ 输入 + 状态 + 结果
2. **状态机** — 创建→排队→运行→成功/失败/超时，可重试可取消
3. **算子编排** — DAG（如 ASR→LLM→TTS→渲染）有依赖有并行

**收尾：** 这块我在项目里也踩过坑——想深入的话，可以接着聊：任务超时怎么判定？您更想看哪个方向？

## 视频脚本

> 预计时长：4 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡 | "音视频异步任务系统怎么设计——这道题面试官到底想考什么？我用 30 秒给你讲透。" | 开场钩子 |
| 0:15 | Agent 编排链路图 | 先说核心：音视频 AI 生成是'用户请求 → 异步任务排队 → 多阶段算子编排（ASR/LLM/TTS/数字人渲染/视频合成）→ 结果回传'的长链路任务系统，核心是状态机和资源调度。 | 核心定义 |
| 0:50 | 概念结构示意图 | 创建→排队→运行→成功/失败/超时，可重试可取消。 | 状态机 |
| 1:20 | 流程图 | DAG（如 ASR→LLM→TTS→渲染）有依赖有并行。 | 算子编排 |
| 1:50 | 代码示例截图 | GPU 池化、优先级队列、抢占/预留。 | 资源调度 |
| 3:30 | 总结卡 | 一句话记忆：异步任务三件套：task_id + 状态机 + 队列。 下期可以接着聊：任务超时怎么判定。 | 收尾总结 |
