---
id: pdd-trade-003
difficulty: L4
category: pdd-trade
subcategory: 多活容灾
tags:
- 拼多多
- 交易
- 多活容灾
- 异地多活
- 单元化
feynman:
  essence: 多活容灾通过"异地多机房+单元化（按 UID 切分）+ 数据同步"让单机房故障不影响全局，拼多多核心交易必须做到同城双活+异地灾备。
  analogy: 多活像连锁店多地开店——一家店着火（机房故障）其他店继续营业（业务不中断），每家店服务本地客户（单元化就近）。
  first_principle: 单机房必然有故障（电力/网络/灾害），多机房冗余让系统级可用性指数级提升。
  key_points:
  - 同城双活（强一致，50km 内，延迟 < 5ms）
  - 异地灾备（最终一致，1000km+，异步复制）
  - 单元化：按 UID 切分，每单元独立完整链路
  - 流量切换：DNS/SLB 秒级切流
first_principle:
  problem: 单机房故障（电力/网络/灾害）会让全站不可用，如何做到机房级容灾？
  axioms:
  - 单机房故障率不可消除
  - 多机房冗余故障率指数下降
  - 不同机房独立故障域
  rebuild: 同城双活（强一致）+ 异地灾备（最终一致）+ 单元化（爆炸半径）+ 自动切流。
follow_up:
- 同城双活怎么强一致？——专线 + 半同步复制（主写备 ACK 才返回）
- 单元化路由怎么实现？——UID hash 路由到单元，跨单元调用用 GZS（全局服务）
- 流量切换怎么平滑？——DNS 权重切换 + 健康检查 + 灰度切流
memory_points:
- 同城双活（强一致，专线半同步）+ 异地灾备（最终一致，异步）
- 单元化：按 UID 切，每单元独立完整链路
- 切流：DNS/SLB 秒级
- 机房间独立故障域
---

# 【拼多多交易】多活容灾怎么设计？核心交易怎么保证不挂？

> JD 依据："核心交易链路多活容灾"。

## 一、多活架构

```
同城双活（上海 A + 上海 B，50km）
  ├─ 强一致（专线半同步复制）
  └─ 单机房挂自动切另一机房

异地灾备（杭州/深圳）
  ├─ 异步复制（秒级延迟）
  └─ 大灾难切流

单元化（按 UID 切）
  ├─ 单元 A: UID 0-5000万（完整链路）
  ├─ 单元 B: UID 5000万-1亿（完整链路）
  └─ 全局服务（GZS）：跨单元（如全局黑名单）
```

## 二、同城双活强一致

```
写请求 → 主库（上海 A）
   ↓ 半同步
备库（上海 B）ACK → 返回客户端成功
（备库没 ACK 则降级异步 + 告警）
```

专线 50km 延迟 < 5ms，强一致可行。

## 三、单元化（爆炸半径控制）

```
单元 A（服务 UID hash 0-49%）
  ├─ 交易服务
  ├─ 订单 DB（A 的数据）
  └─ Redis（A 的缓存）

单元 B（服务 UID hash 50-100%）
  └─ 完整独立链路
```

**好处**：单元挂只影响 50% 用户（而非全站）；横向扩展加单元。

**跨单元**：全局数据（如全局黑名单）用 GZS，理论上业务不该跨单元调用。

## 四、流量切换

```
机房故障检测 → SLB/DNS 切流到备机房
  ├─ 4xx/5xx 率突增 → 告警
  ├─ 心跳失败 → 摘除
  └─ 灰度切流（10% → 50% → 100%）验证
```

## 五、底层本质

多活容灾本质是**"用冗余换可用性"**：
- 单机房故障率 1% → 两机房 0.01%（独立故障域）
- 单元化限制爆炸半径
- 自动切流秒级恢复

这是金融核心系统的硬要求——拼多多核心交易必须 99.99%+ 可用。

## 常见考点
1. **同城和异地多活选哪个**？——强一致场景（交易）同城双活；容忍最终一致选异地多活。
2. **单元化最难的是什么**？——跨单元调用（不该有）+ 全局数据同步 + 单元迁移。
3. **多活的成本**？——机房+专线+数据同步延迟，通常核心业务才值得。
