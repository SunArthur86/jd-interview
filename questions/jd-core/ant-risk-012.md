---
id: ant-risk-012
difficulty: L3
category: jd-core
subcategory: 风控系统
tags:
- 蚂蚁
- 风控
- 规则引擎
- Drools
- 决策树
feynman:
  essence: 规则引擎把"业务策略"从代码里解耦成"配置化规则"，运营可动态调，避免改代码上线，是风控决策的核心组件。
  analogy: 规则引擎像一个"如果...那么..."的查询机：输入用户特征（年龄、金额、设备），按预设规则匹配，输出风险决策（放行/拦截/复核）。规则可配置不用改代码。
  first_principle: 风控策略迭代频繁（每天可能改几十条规则），硬编码上线周期长、风险大；把规则抽象成数据，让"策略变化"和"系统代码"解耦。
  key_points:
  - 规则引擎 = 规则定义 + 执行引擎 + 决策输出
  - DSL：Drools DRL、QLExpress、Aviator、自研脚本
  - RETE 算法：构建网络，匹配所有规则
  - 决策树/决策表：可视化规则组合
  - 规则版本、灰度发布、A/B 实验
first_principle:
  problem: 风控策略要快速响应新型欺诈（每天变化），但系统代码改动上线周期长、风险高，怎么解耦？
  axioms:
  - 业务策略（规则）变化频率 >> 系统代码
  - 代码改动有发布成本和故障风险
  - 规则本质是"特征+条件→决策"的映射
  rebuild: 引入规则引擎——规则用 DSL 配置（运营可改），引擎执行（系统稳定），二者解耦。规则变更通过热加载或版本发布，不动系统代码。
follow_up:
- 你用过什么规则引擎？——Drools（功能强但重）、QLExpress（阿里轻量）、Aviator（表达式）、自研决策树
- 规则多了性能怎么优化？——RETE 算法共享公共模式；规则按特征分桶；特征预计算
- 规则冲突怎么处理？——优先级、规则互斥约束、决策矩阵
memory_points:
- 规则引擎 = 规则 DSL + 执行引擎 + 决策输出，解耦策略与代码
- RETE 算法：编译规则成网络，复用公共条件，匹配效率高
- 规则热加载（不重启）+ 版本管理（灰度、回滚）是生产必备
- 风控规则常按"专家规则 + 机器学习模型"组合决策
---

# 【蚂蚁风控】讲一下风控规则引擎怎么设计？用什么实现？怎么让规则动态生效？

> JD 依据："数字化风控服务及智能风控平台"。规则引擎是智能风控平台的核心。

## 一、表面层：规则引擎是什么

**硬编码风控的痛点**：
```java
// 业务方提需求：新加一条规则"夜间大额交易拦截"
if (amount > 5000 && time > 22:00 && time < 06:00) reject();
// 要改代码、单元测试、CR、上线... 周期 1-2 天
```

**规则引擎的解法**：
```
配置后台: 加规则 →"amount>5000 && time∈[22,6] → REJECT"（5 分钟生效）
风控引擎: 读规则 → 执行 → 返回决策
```

业务运营自助配置，不用改代码。

## 二、规则引擎的核心三件套

**1. 规则定义（DSL）**：
```
rule "夜间大额交易"
when
    $e : RiskEvent(amount > 5000, hour >= 22 || hour < 6)
then
    $e.setDecision(REJECT);
end
```

**2. 执行引擎**：
- 加载规则 → 编译成执行计划 → 输入特征 → 匹配规则 → 输出决策

**3. 决策输出**：
- `PASS`（放行）/ `REVIEW`（人工审核）/ `REJECT`（拦截）

## 三、主流规则引擎对比

| 引擎 | 特点 | 适用 |
|------|------|------|
| **Drools** | 功能强大（RETE 算法）、社区成熟 | 复杂规则、传统行业 |
| **QLExpress**（阿里） | 轻量、Java 友好、阿里系常用 | 阿里系、电商 |
| **Aviator** | 表达式引擎（非完整规则引擎） | 简单表达式 |
| **Easy Rules** | 极简 | 教学 |
| **自研决策树** | 业务定制强 | 强业务定制（如风控） |

**风控的选型**：
- 蚂蚁用 **QLExpress** + 自研可视化决策树（运营拖拽配置）
- 大部分互联网公司自研（业务定制强）

## 四、RETE 算法（Drools 的核心）

RETE 是规则匹配的高效算法，核心是**规则网络**：

```
规则1: amount > 1000 AND status = 1 → 决策A
规则2: amount > 1000 AND hour > 22  → 决策B
规则3: amount > 5000 AND status = 1 → 决策C

RETE 网络（共享公共条件）:
   [amount > 1000] ──┬── [status = 1] → 规则1
                     └── [hour > 22]  → 规则2
   [amount > 5000] ──── [status = 1] → 规则3
```

- 公共条件（如 `status = 1`）只算一次，结果复用
- 适合规则数多的场景（百千条规则）
- 缺点：内存占用大、规则变更需重建网络

## 五、风控规则引擎架构

```
风控决策请求 →
    │
    ├─ 1. 特征查询（拉用户画像、交易历史、设备指纹）
    │
    ├─ 2. 规则匹配
    │   ├─ 前置规则（黑名单：硬拦截）
    │   ├─ 名单规则（白名单：直放）
    │   ├─ 专家规则（RETE 匹配）
    │   └─ 模型规则（ML 模型打分）
    │
    ├─ 3. 决策融合
    │   按优先级 + 加权 + 阈值 → 最终决策
    │
    └─ 4. 输出
        ├─ 决策（PASS/REVIEW/REJECT）
        ├─ 命中规则列表（explainable）
        └─ 风险分（0-100）
```

## 六、规则动态生效（关键工程问题）

**1. 规则存储**：
- 数据库存规则定义（rule_id, dsl, priority, version, status）
- 配置中心存规则版本（Nacos）

**2. 热加载**：
```java
// 监听配置变化
@NacosConfigListener(dataId = "risk-rules")
public void onRuleChange(String rules) {
    KieSession newSession = compile(rules);   // 编译新规则
    KieSession oldSession = currentSession;
    currentSession = newSession;              // 原子切换
    oldSession.dispose();                     // 释放旧 session
}
```

**3. 版本管理**：
- 每次规则变更生成新版本号
- 支持灰度（10% 流量用新规则）
- 支持回滚（一键切回旧版本）

**4. A/B 实验**：
- 同一请求按 uid hash 分桶
- 不同桶用不同规则版本
- 对比拦截率、误杀率、漏报率

## 七、性能优化

**1. 规则分桶**：
- 按业务类型分（借贷、支付、营销）
- 每类独立规则集，避免全量规则匹配

**2. 特征预计算**：
- 用户画像、历史统计预先算好
- 实时只查不用算

**3. 短路评估**：
- 高优先级规则（黑名单）先评估，命中即返回
- 避免低优先级规则全部执行

**4. 增量编译**：
- 只编译变更的规则
- 不重建整个 RETE 网络

## 八、可解释性（风控的硬要求）

风控决策必须可解释（监管要求、用户申诉）：
```
决策: REJECT
命中规则:
  - R001: 短时间内异地登录 (score=30)
  - R045: 设备指纹关联多个账号 (score=40)
  - R123: 交易金额超过历史均值 10 倍 (score=30)
合计: 100 ≥ 阈值 80 → REJECT
```

每条决策必须保留：输入特征、命中规则、规则版本、决策时间（用于审计和申诉）。

## 九、底层本质：业务与代码的解耦

规则引擎本质是**"业务逻辑数据化"**：
- 把"业务策略"从代码里抽象出来
- 用 DSL（领域专用语言）表达
- 让业务方能自助配置

这是**关注点分离**的工程实践：
- **系统工程师**：保证引擎性能、稳定性
- **风控运营**：保证规则有效性、迭代速度
- **数据科学家**：保证模型精度

类比：
- 工作流引擎（Activiti）：流程数据化
- 规则引擎（Drools）：策略数据化
- 配置中心（Nacos）：参数数据化

**和 AI 的关系**（见 jd-ai 批次）：未来规则引擎会被 AI Agent 替代——AI 实时分析场景、动态生成策略，但"决策可解释、规则可审计"的要求让传统规则引擎在金融场景仍长期存在。

## 常见考点
1. **RETE 算法为什么快**？——把规则编译成网络，公共条件只算一次，规则增加时性能不线性劣化。
2. **规则引擎 vs 决策树模型**？——规则引擎是人写的精确逻辑，决策树是数据驱动的统计模型；实际是组合使用（专家规则 + ML 模型融合决策）。
3. **规则变更导致线上事故怎么办**？——必须灰度（小流量验证）+ 回滚（一键）+ 监控（拦截率异常告警）。

**代码示例**（QLExpress 集成）：
```java
ExpressRunner runner = new ExpressRunner();
// 运营在后台配置的规则表达式
String rule = "if (amount > 5000 && hour >= 22) { return 'REJECT'; } return 'PASS';";

DefaultContext<String, Object> ctx = new DefaultContext<>();
ctx.put("amount", 6000);
ctx.put("hour", 23);
String decision = (String) runner.execute(rule, ctx, null, false, true);
```
